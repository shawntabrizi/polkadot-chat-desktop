# Review M12d — PASS (2026-09-24)

`docs/milestones/M12d.check.sh` → `CHECK PASS: M12d` (480 tests, tokens clean, smoke, typing e2e). Render count: 10 deltas in a 60-message room re-rendered the other bubbles 610 times before, 0 after; the streaming bubble 21 times. Long tasks during a 1398-character reply at 50 deltas/s: 0 at normal CPU on this Mac before and after; at 4× slowdown 5 → 0, at 6× 68 → 0. The second reveal (painted text dropping by 1190 characters at completion) is gone.

Causes confirmed with one correction: markdown is parsed for incoming bubbles only (about 30 per delta in a 60-message room, not 60); React kept the markdown element at completion and only the keyboard re-mounted. Both recorded in decisions.

Choices accepted: stable row and action objects in MessageFlow instead of `useCallback` in Room; a small test-only fake DOM; a late delta after the end is ignored; the keyboard slot is reserved while streaming so completion moves nothing.

Owner check needed: the recording was made on the owner's Mac while the old build ran; the stall there was likely the whole-room re-render plus the second reveal. Confirm with the new dmg.

Carry: `e2e:typing` printed "inside the 5 s window: 1" once (M12c printed 0); the script passed; watch it in M12e's run.
