# Review M12f — PASS (2026-09-24)

`docs/milestones/M12f.check.sh` → `CHECK PASS: M12f` (525 tests, tokens clean, smoke, meter e2e: `HEADER_MATCHES_BALANCE`, header "5 PAS (~50 replies)", tooltip "5.1 PAS on chain · 0.1 not yet charged"). pca half 2038330 (pending rides the reply's batch, persisted debit; live: one submission carrying reply, botInfo and seen). Vectors 0008c pinned on both sides.

Answers to docs/questions.md "## M12f":
1. Spec 0008: "latest version wins" → "a botInfo whose version ≥ the stored one replaces it" (reviewer edits the spec).
2. The few-second window after a charge where the header reads low: acceptable.
3. Forwarding to the Assistant does not open its room: keep; the reply shows as unread on the Assistant row.

Carry: pcdmeter moves to the VPS stack (profile `later`) now that M12f is on both sides.
