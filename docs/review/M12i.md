# Review M12i — PASS pending the combined check (2026-09-24)

Commit b9b0c88. Evidence: 566 tests, tokens clean, smoke, `e2e:demo` with a fresh identity: 7 of 7 bots accepted within 8.5 s, greeted 7/7, second run sent 0 (`DEMO_IDEMPOTENT`). Screenshot reviewed (night onboarding): the "Meet the demo bots" pane, seven rows with tag chips and taglines, "Start chats with all" as the one pill, ghost Skip; row states Sent / No answer yet.

Note: b9b0c88 also carries the M12g agent's staged lines in shared files (ipc, preload, desktop-api, package.json, screenshots, docs, one line in Shell.tsx), so main type-checks only once M12g commits. The M12i check runs after M12g lands.

Answers to docs/questions.md "## M12i":
1. Tags are right.
2. The step shows once; Settings › Demo is the way back.
3. Match the labels: the chat-list row should also read "Sent · just now" for 15 s before "No answer yet". Small; next fix round.
4. "Remove demo chats" keeps the contacts (consistent with M12e delete); a later start sends "Hi!" as a message, which is fine.

Username resolution through the People chain read (`Resources.UsernameOwnerOf`) instead of the backend search: good; note it as the preferred exact lookup everywhere.
