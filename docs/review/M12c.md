# Review M12c — pca half PASS (2026-09-24); desktop half pending

## pca (`desktop/rfc-0003` c488a31 + 0907cde)

Verified by the reviewer: `npm test` in bot-core 563 tests, 558 pass, 0 fail, 5 skipped (t3ams SDK, skipped before). All seven bots restarted on 0907cde and alive. Live proof from the agent: pcdpirate answered a question with one statement carrying `seen` and `text`, zero `typing` since the restart; pcdmeter charged once after five replies (block 13624072) with one status-1 reference; the faucet drip posted one reference.

Answers to the agent's questions:
- Bots opting in to typing: the spec says a bot MUST NOT send it. The explicit `BOT_PROTOCOL_EXTENSIONS` switch may stay as a test-only knob (the group fan-out test uses it); the docs must say "test only". No bot in the fleet runs with it.
- Pending debit across a crash: in memory is acceptable on devnet (a loss favours the user). Persist it in session state before any mainnet use. Carry.
- Placeholder delay gated on `botinfo` or `typing`: intended. The client's local "working" state needs the bot info.

Noted: the botInfo balance hint cannot show the pending debit (the client reads `balanceOf` from the chain). `/balance`, the refusal and the reference note subtract it. `efficiency.md` corrected.

## Desktop (1501b72 + 12c1e9f) — PASS (2026-09-24)

`docs/milestones/M12c.check.sh` → `CHECK PASS: M12c` (469 tests, tokens clean, smoke, e2e typing `BUDGET_OK`: our question = 1 submission, the read inside the 5 s window = 0, the bot's seen rode its reply, local "working" showed at 0.0 s and cleared on the reply). Probe on devnet: up to 4 statements/s from one sender, 160 sent, 160 received, p50 ≈ 330 ms, p95 ≈ 370 ms, no rejection; the ceiling is above this. Screenshots reviewed: `room-tx-done` shows the short hash in mono, "Copy hash", "View on Subscan", and "finalized in block #…"; the footer block; the Chat settings with "Send typing indicators" off and "Block explorer".

History note: the reviewer's commit 1896d62 swept the agent's in-progress source into it (`git add -A` while the agent worked). All M12c code is in history across 1896d62, 1501b72 and 12c1e9f. Rule from now: the reviewer adds paths, never `-A`, in a repo where an agent is running.

Answers to docs/questions.md "## M12c":
1. A slow transaction gets status 0 at 30 s and then its end state: right (two references only for slow ones).
2. Acknowledgements: count them in Diagnostics (done) and in `efficiency.md` as a base-protocol cost, one per fetched batch. A batched or piggybacked ACK is an upstream question for chat-spec, noted for the submission.
3. Everyone's typing is off after the update: correct, that is the ruling.
4. `seen` rides any content: correct.
5. Polkadot.js Apps has no account page: the disabled button with a tooltip is right.
6. Pocket Copy: fixed by the reviewer in 12c1e9f (same hook).
7. Step 10 added to the milestone by the reviewer.

Carry (next fix round, before M13):
- Screenshot runs take 10–15 min: one flow per state with both themes captured by switching the attribute; a `--only` flag; fixtures for pure UI states (reference states, keyboards, diagnostics); a seeded profile for shots that only need existing rooms; parallel flows.
- Diagnostics counts reset on window load; keep them in the main process so a theme switch does not zero them.
- pca: pcdflip logs a statement-ingress heartbeat recovery every 30 s since its restart (76+), while still receiving. Find the cause.
- Persist the pca meter's pending debit; the room header cannot show it.
