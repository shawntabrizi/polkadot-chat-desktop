# Review M19 — PASS; release ready, publish pending the repo going public (2026-09-24)

Commit 87e9483. 851 tests, tokens clean; headless smoke; packaged smoke `SMOKE_OK` + `FRESH_OK` + `AGENT_SELFTEST_OK`; `e2e:profiles` on devnet with the new reveal and restore steps (`REVEAL_OK` after typing "reveal", `RESTORE_OK` yields the same account, a second restore refused). The migration code is gone (a data folder without `profiles.json` is a fresh install; folders without an index are re-listed, never moved). README has the unsigned-app install steps. CHANGELOG 0.2.0-preview written from the reviews. `scripts/release.sh v0.2.0-preview` on main: `RELEASE_READY`, dmg sha256 1d42a16e…6838, notes at `dist/release-notes-v0.2.0-preview.md`.

Publish step (coordinator, after the owner makes the repo public):
`gh release create v0.2.0-preview --prerelease --title "Polkadot Chat v0.2.0-preview" --notes-file dist/release-notes-v0.2.0-preview.md "dist/Polkadot Chat-0.2.0-arm64.dmg"`

Answers to docs/questions.md "## M19": clear the clipboard when the phrase auto-hides: yes, next round; README links point at shawntabrizi/polkadot-chat-desktop, which stays the public home.
