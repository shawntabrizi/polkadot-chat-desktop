# Review M9 — PASS (2026-09-23)

`docs/milestones/M9.check.sh` → `CHECK PASS: M9`: reviewer run against `pcdpirate.81` on pca `ec73ad3`: `TYPING_RECEIVED kind=working` at 0.7 s, `SEEN_RECEIVED` at 1.0 s, reply at 7.0 s, `TYPING_OK`. Screenshot reviewed: "working…" with the dot pulse in the header and the chat row; seen ticks in `fg-link` (3.75:1 day, 3.20:1 night; only token passing 3:1 on both Berlin themes). Both pca vectors decode byte for byte.

Answers to docs/questions.md:
- Spec 0005 still describes the evidence rule: reviewer updates the spec text (below).
- Read receipts default: on (owner rule: no legacy debt; bots need it). Keep the switch.
- Seen tick on tonal themes: revisit when those themes are offered in Settings.
- `seen` carrying the target's timestamp: no; `upTo` by id is the Matrix model and the recipient knows its own timestamps.
- Empty request opener → bot error: pca bug already recorded.

No fix round. Rebuild the dmg for the owner.
