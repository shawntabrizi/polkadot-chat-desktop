# Review M5 — PASS with fixes (2026-09-23)

`docs/milestones/M5.check.sh` → `CHECK PASS: M5`. Reviewer looked at `berlin-night/chats.png`, `berlin-night/room.png`, `berlin-day/signup.png`, `berlin-day/assistant.png`, `berlin-night/settings.png`, `berlin-day/requests.png`. Two panes, tokens, grouped bubbles with tail corners, Lucide ticks, markdown, request banner, settings sections: all as specified. Shawn reviewed the sign-up screenshot.

Fix list (commit as `M5: review fixes`):
1. **Username and number as one field, like the phone** (Shawn, 2026-09-23; source `polkadot-app-android-v2` `feature/usernames/api/.../compose/UsernameTextField.kt`): the username `Input` gets an inline suffix inside the same field: a "." in `text-fg-tertiary` followed by a small two-digit input (min width 28px, `font-mono`, `text-fg-primary`) that is pre-filled with the first `availableDigits` entry once the name is available and is editable. Remove the "Number" Select and the "Let the network pick" option entirely: the claim always sends the digits shown. When the typed digits are not in `availableDigits`, the field border turns `stroke-error` and the status line reads "Digits taken. Try again." (mobile string). The suffix is hidden until the name is valid and checked. The status line under the field keeps "It's yours!" / "Taken. Try another." / "Minimum 6 characters". Keep the Network select where it is.
2. Re-run `npm run screenshots` for `signup` in both themes and look at them; update the M5 section of `docs/acceptance.md` with the new paths.

Accepted as is: opal avatar tone on Berlin Night (theme-invariant by design); Danger section below the fold in Settings; the `next-themes` alias shim for Sonner.

Bot crash during the run: `pca` 0.10.0 `outbound-lanes.mjs:179` (`lane.current` null). Reviewer restarted the bot and recorded the bug in the vault; not this repo's problem. `domain/identity/reset.ts` is now unused: delete in the next domain milestone.
