# Review M1 — PASS with fixes (2026-09-23)

`docs/milestones/M1.check.sh` → `CHECK PASS: M1` (registered `pcdrevchibacbfcc.48` on devnet, `ON_CHAIN key_type=0`, on-chain key matches). Read `service.ts`, `store.ts`, `ipc.ts`, `preload/index.ts`, `SignUp.tsx`, `docs/decisions.md`, `docs/questions.md`.

Answers to docs/questions.md:
- Check names are letters only now (reviewer changed the check script).
- Logout: remove the "Log out" control in M1. M3 adds "Reset identity" behind a confirm (see docs/milestones/M3.md step 2).
- Settings labels: fix now (item 2 below).

Fix list (do only these, commit as `M1: review fixes`):
1. Remove the "Log out" button and its handler from `src/renderer/ui/Settings.tsx`. Leave the underlying Dexie clear function in place for M3.
2. In `Settings.tsx`, replace the pairing wording for a self-owned identity: show a section "Identity" with the username (`name.NN`), the account (hex, with a Copy button), the network, and the line "Created on this computer". Remove "Paired", "Phone device", "Paired at" and the phone key rows. Keep the network select. Add a short spec if Settings has one already; otherwise none.
3. In `docs/acceptance.md` under "## M1", add one line stating the reviewer's check run: `pcdrevchibacbfcc.48`, `ON_CHAIN key_type=0`.

Carry to M3: `extraResources` for the wasm in the packaged app (agent noted it); a CSP meta tag.
