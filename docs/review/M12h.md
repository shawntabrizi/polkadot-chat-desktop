# Review M12h — PASS (2026-09-24)

Commit 2305576. Full screenshot pass: 352 s and incomplete before, 78.8 s complete after (72 PNGs); one local-rows shot 3.5 s; a chain shot 18 s (Asset Hub connect). Both themes captured from one state; fixture rows for UI states; four headless workers in parallel. Carry items landed with tests: "Sent · just now" for 15 s, verified incoming send ("checking…" until the chain shows it), single amount on the Pay button, reply rows in search, over-limit keyboards rejected.

The check on main failed once: the script needed `PCD_SCREENSHOT_IDENTITY`; fix requested (defaults in the script), then the check reruns.

Answers to docs/questions.md "## M12h":
1. The spinner state on room-buttons is not needed as a shot.
2. "not found on the chain" is fine for a send the chain does not show; add "yet" while the reference is under 2 minutes old.
3. The second flip stake failing with `StorageDepositLimitExhausted` after a passing dry-run: a pca/contract item (the dry-run and the submit read different state on a lagging node); carried to pca.
4. room-tx-done drops the Meter header; room-meter keeps it.
5. Spec 0006: a keyboard over the limits is rejected as unsupported (added by the reviewer).

Reviewer rerun on 7b18368 (identities default in the script): `CHECK PASS: M12h`.
