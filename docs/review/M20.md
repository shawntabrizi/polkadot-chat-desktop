# Review M20 — PASS, verified with the owner's phone (2026-09-24)

Desktop 82f5d69, pca b8b9fc4. Capabilities (kind 252) keyed per peer device on both sides; extensions gated on the advertised set; kind 250 read-only; photos as `RichText` + Bulletin `FileVariant` 1 to capable peers and over HOP in the phones' envelope to baseline peers; existing chats send the set once with the next message. `e2e:caps` live: two desktops exchanged sets and used Bulletin + seen; a baseline bot got a numbered menu and a HOP photo. 928 tests. Owner's phone: a desktop photo shows on the phone; a phone photo shows on the desktop.

Rulings on the agents' questions: the accepting desktop sends its set with the accept (no extra statement); a "delete for everyone" row to a baseline phone says "removed here; the phone keeps it"; ask the SDK team for a sending-device field (the desktop decrypts each incoming statement twice to find the device); the devnet `//Eve` Bulletin budget is exhausted (fresh identities cannot store files) — a devnet operator question for the morning review; groups are not yet gated on capabilities (they are desktop/pca only today).

Carry: own messages render markdown like a peer's (owner asked); e2e:hop needs a Bulletin-authorized identity while `//Eve` is empty.
