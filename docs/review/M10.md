# Review M10 — PASS (2026-09-23)

`docs/milestones/M10.check.sh` → `CHECK PASS: M10`: the guide bot sent `botInfo` ("Polkadot Guide", 5 commands, kind agent) on accept and again on `/start` with its greeting. Screenshots reviewed: badge + description subtitle + `/` command popover in the room; the Faucet contact with its two buttons and the host confirm strip; a Bots section in search. Vector decoded byte for byte.

Answers to docs/questions.md:
- The `/start` heuristic (identity-channel content but no botInfo) is fine for now.
- Faucet targets Paseo Asset Hub (parachain 1000, devnet) — correct for the current network.
- Faucet room without a composer: keep.

Carry: open the faucet URL once by hand to confirm the address prefills (owner). Next: M11 needs the contract decision; M12 publish-agent.
