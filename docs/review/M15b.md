# Review M15b — PASS on the desktop steps; the bot-describe step waits for an operator decision (2026-09-24)

Commit 4345b70. 731 tests, tokens clean, smoke. `e2e:attach` on devnet: image (bitswap + gateway), a 2.3 MB file in two chunks (gateway first for the 2 MB chunk, bitswap for the 300 KB one), an album of 4 images in one statement and 4 Bulletin transactions, a 60 s voice note (WebM/Opus, 32 bars; over 5 min refused), every hash verified. Screenshots: file row, album grid, voice player (a real WebM/Opus recording played back), plus the M15a shots retaken.

Bot step: `BOT_DESCRIBE_FAILED`. The fleet's pcdguide.70 runs with `BOT_AI_TOOL_CAPABILITIES=` (none), so Claude cannot open the staged image and answers "I can't view images without tools". The stricter describe check is right (a greeting or a refusal no longer counts). **Owner decision:** allow `BOT_AI_TOOL_CAPABILITIES=read` with `BOT_AI_TOOL_SCOPE=workspace` on pcdguide (read-only, confined to the staged attachments and its workspace, inside the container); the coordinator's attempt to set it was blocked by the permission classifier as a policy change on a public bot. Command when approved (runbook VPS.local.md): edit `bots/pcdguide/bot.env` on the VPS, then `docker compose -p pca-demo --profile claude up -d --force-recreate pcdguide`, then rerun `npm run e2e:attach`.

Answers to docs/questions.md "## M15b":
1. Above.
2. Spec 0012 accepts WebM/Opus and Ogg/Opus for voice notes (reviewer edits the spec).
3. The keys table goes to M16b with a migration for attachment keys (already in M16b's brief).
4. Video: M15c, as a file with a poster thumbnail and inline playback.
5. Album chunks may be stored in flight together as long as each item has its own key and nonce (the nonce collision concern is per item, not across items); M15c.


Resolved 2026-09-24 morning: the owner approved read+web on the public Claude bots after pca a0ba269 added per-peer staging and the web egress guard; `e2e:attach` is fully green with `BOT_DESCRIBE_OK` ("A bright red circle sits on a noisy gray background.").
