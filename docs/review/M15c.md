# Review M15c — PASS (2026-09-24)

Commit 56e55a7. 779 tests, tokens clean, smoke. `e2e:attach` on devnet: image, file, album (one store call, 4 transactions), voice, video (900 KB webm with a poster, gateway first), ask-to-resend (one statement) and resend (same CIDs, nothing resubmitted since devnet still held the chunk, the receiver fetched again by the same CID). Bot step `pending-operator` until pcdguide gets read tools (morning decision). Screenshots: video, storage panel with the live grant (49.4 of 64 MB, 62 of 100 transactions, refill block), attachment shots retaken; `room-voice` not captured because this Mac's audio clock does not advance in headless playback (the voice code is unchanged from M15b).

Accepted: attachment keys in the `keys` table as `att:<messageId>:<index>` sealed at rest, stripped from the message row, migrated once at start; the `#resend/<id>` fragment link; resend refuses a changed file; the client budget is a warning, never a refusal; Free space frees received copies only.

Answers to docs/questions.md "## M15c":
1. Free space may also offer sent copies older than 14 days (after Bulletin retention), as a second toggle. Later.
2. The daily share stays a warning.
3. Videos carry their file name (they are files); photos stay nameless.
4. Spec 0012 names the `#resend/<messageId>` fragment form as the host convention (reviewer edits); `replyTo` is not needed.
