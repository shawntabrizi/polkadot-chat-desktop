# Review M16 — PASS (2026-09-24)

`docs/milestones/M16.check.sh` → `CHECK PASS: M16` (670 tests, tokens clean, smoke, `e2e:group2` GROUP2_OK in 84 s on devnet with a throwaway pca echo bot: create in one statement, ONE_SUBMISSION per message, bot reply in one statement, carry after a restart, removal in two statements with the removed member locked out of epoch 2, history page from the bot, a v1 room migrated in place). pca half 0fa12a2 (GROUP2_LIVE_OK). Vectors 0011 match on both sides except one printed AAD length (see below).

**The finding that matters most (spec 0011 Unresolved 3, now seen live):** the two busiest test identities (pcde2e, pcdeceb) cannot post in any v2 group: `AccountFull`. Their DM statements never expire (`u32::MAX`) and fill the 50-statement allowance the identity backend grants. So today an identity that has chatted with about 25 peers can never post in a group. This is a base-protocol and allowance question, not a groups question; it needs an upstream answer (see efficiency.md "Allowance facts") and a client mitigation. Proposed mitigation for the morning review: **slot garbage collection**: when a DM message has been ACKed and the channel is idle, the sender replaces its slot with a short-expiry statement so the slot frees itself; done lazily only when the account is near the limit, so the ordinary cost stays one submission per message. Alternative: ask chat-spec for a DM expiry below `u32::MAX` (ACKed statements need not live forever).

Accepted choices: "New group" creates v2 (v1 kept behind an option for the old e2e); a group statement never raises its expiry above a DM's after `AccountFull`; history request/response built (ruling 1); epoch keys on the `groups` row for now.

Answers to docs/questions.md "## M16":
1. vectors-0011.md (b) prints a 41-byte AAD; the 40-byte AAD both codecs compute is authoritative; the reviewer marks the line in the vectors file and asks pca to reprint it.
2. Ruling 8 (botInfo rides the bot's first group statement) is not in pca 0fa12a2 → pca follow-up.
3. Epoch keys move to their own table next to `secrets` in M16b.
4. `deviceAdded` inside a group message waits for M16b.
5. A welcome from a contact adds the group at once (Telegram's default); a welcome from a stranger shows as an invite to accept (M16b, with the join flow).
6. The quota: above.

Carry to M16b: invite links, join requests (+ bot-admin approval over DM with buttons), shared history UI, roles and permissions UI, pins, slow mode, `/remove` for a bot admin, keys table, deviceAdded, stranger invites.
