# Review M12 — PASS (2026-09-23)

`docs/milestones/M12.check.sh` → `CHECK PASS: M12` on the second run. The first run failed with `identity lookup timed out after 15000ms` on every request (People chain RPC, not the group code); the rerun and the agent's three earlier runs all reached `GROUP_OK`: two desktop peers and pcdguide.70 in one group, fan-out, one bot envelope on both peers, roster v2 removal. Vectors 0009 encode and decode to pca's bytes. Screenshots reviewed: group room with named senders, members panel (Remove on hover, Leave as Danger), Faucet room with the in-app drip from //Alice and "Balance now", footer account block.

Owner feedback closed in this milestone: footer self-block, embedded Faucet with progress, streaming fence placeholders.

Answers to docs/questions.md:
- README/kinds for 0009: reviewer updates (done in this commit).
- Reactions by several members count once: acceptable in v1; a per-account reaction list goes with groups v2 (group topic), not before.
- `tx` buttons in groups: bots SHOULD NOT send them in groups in v1; pca's group context says so. Multisig-style flows come with groups v2.

Carry:
- Identity lookup timeout (15 s, no retry) drops requests on a slow People RPC. Add one retry with backoff before dropping. → M12c.
- `berlin-night/room-tx-done.png` missed once (self-transfer stayed "submitted" 90 s while the day run took 1.5 s). Watch; no action.
- pca: serialize faucet sends; second-accept stall; RFC-0003 number 20→21 upstream.
