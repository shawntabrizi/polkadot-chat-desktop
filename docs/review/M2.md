# Review M2 — PASS (2026-09-23)

`docs/milestones/M2.check.sh` → `CHECK PASS: M2`: reviewer run reached `REQUEST_SENT / ACCEPTED devices=1 / REPLY Echo: ping fc50e7 / E2E_OK` against `pcdpeer.47`. Read `docs/decisions.md` M2, `docs/questions.md` M2, the diff stat.

Notable fixes found by the agent, all correct: `heartbeatTimeout: Infinity` (drops the socket at once in Node and Chromium), SS58 passed where host-papp wants hex (every lookup failed), device key must differ from the identity chat key (bot decrypted with the wrong secret), username search moved to `/api/v1/usernames/search` behind proof of compute.

Answers to docs/questions.md:
- Stale device entries at bot-core: upstream SDK question (`unwrapForOwnDevice` takes the first entry). Not ours to fix now; recorded in the vault.
- Owner identity `shawntest.76` in the dev userData: keep it; it is Shawn's.
- e2e flakiness at the first storage read: fix the cause, not the test. See M3 step 1 (metadata cache + endpoint fallback + best block).

No fix round. Carry to M3 (already in docs/milestones/M3.md step 1): best-block reads, a metadata cache like bot-core's so the first read does not stall for minutes on public nodes, fallback to the next People endpoint when a read times out, and wrap host-papp's `Consumers` read so the app reads at best block.
