# Review M12e — PASS (2026-09-24)

`docs/milestones/M12e.check.sh` → `CHECK PASS: M12e` (515 tests, tokens clean, smoke, typing e2e `BUDGET_OK` with the seen inside the 5 s window = 0, chat e2e against pcdpeer, screenshots with the new `--only` flag in 12 s using fixture rows). Delete, withdraw, archive, pin, unread, clear, nickname, forward, block and the blocked list are in; the desktop's buttons parser now matches pca's leniency; Polkadot.js option captioned; diagnostics counters live in the main process.

Noted: the app had no block feature before this milestone (the roadmap was wrong); it was added with a `blocked` table. One smoke run opened the owner's dev profile for ~1 s without the headless flags and wrote only Chromium housekeeping files; identity and database untouched. Rule restated in AGENTS.md: every app launch by an agent sets `PCD_HEADLESS=1` and `PCD_USER_DATA_DIR`.

Answers to docs/questions.md "## M12e" (reviewer defaults; owner may overrule in the morning):
1. After a withdraw this device does not see a late accept: acceptable. A peer who accepts late and writes shows up as a new request the next time they request; document it.
2. Delete keeps the contact: yes (Telegram does the same).
3. A blocked peer still sees "delivered" because the store ACK is the delivery signal: acceptable now; the alternative (not ACKing) would keep the statement in our slot. Upstream note for chat-spec.
4. An archived chat comes back on a new message unless it is muted (Telegram's rule): change → M12f.
5. Pinned chats go above the Assistant and the Faucet (pinned means top): change → M12f.
6. Forward offers the Assistant as a target (ask the agent about a message): change → M12f.
