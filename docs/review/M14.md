# Review M14 — desktop half landed; live DAO e2e pending devnet (2026-09-24)

Desktop 37cf8d5 (rebased onto M15c): `tx` buttons pressable in v2 group rooms, the proposal card (tally, countdown, vote state, Execute/Withdraw), pinned; the invite scheme switched to `polkadot-chat://`; a `joinedVia` contact's stranger mark clears on our first message. 794 tests, tokens clean, smoke. Screenshots `room-dao` reviewed by the agent. pca half fb7d400 + 2ef2aa7 (`DAO_LIVE_OK` on devnet).

`e2e:dao` is `DAO_E2E_PENDING`: the run reached GROUP_READY (group, member, bot admin, treasury funded) and the bot put proposal #4 on chain, but the bot's group statement was rejected `noAllowance`: the devnet identity backend took over 5 minutes (once 22 minutes) to attest new identities tonight, and the Asset Hub stalled twice. The pair pcdbenchfinb/pcdbenchfina is now AccountFull too. Rerun `npm run e2e:dao` when devnet is responsive; the script now waits up to 15 min for the allowance and retries registration.

Carry (docs/questions.md "## M14"): the card parses the bot's English wording (give the bot a small structured `proposal` line, or a botInfo hint, in the DAO fix round); status 0 is not sent in groups (fine: one reference per tx); a quoted reply in a group names the group instead of the member (fix in the next group round).

The night's two AccountFull identities and the slow attestation make the quota decision (morning review item 1) the first thing to settle.
