# Review M16b — PASS (2026-09-24)

Desktop 7b911bd + 94918e0; pca 96bf004. `docs/milestones/M16b.check.sh` → `CHECK PASS: M16b` (760 tests, tokens clean, smoke; `e2e:group2b` on devnet in 80 s: join by invite under policy 1 with approval, history shared to the newcomer, pin in one statement, slow mode held locally and a forged early carrier hidden, promotion with flags 0xbf, the bot removed by the new admin in two submissions). Regressions green: group2 and v1 group e2e. Screenshots: invite, roles, pinned.

Rulings on docs/questions.md "## M16b":
1. **Invite links get their own scheme.** Registering `polkadotapp://` would make the desktop take the phone app's pairing links. Ruling 9 is amended: invite links are `polkadot-chat://g#<InviteLink base64url>`; the phone app may map its own scheme later; an https wrapper a host owns stays allowed. Desktop follow-up: switch the registered scheme (packaged app only, as built) and the link text. pca: `/invite` answers with the new form.
2. Attachment keys move to the `keys` table in M15c.
3. No rotation at join; the cost table holds; spec 0011 text corrected.
4. The history line wording "History shared by <name>" stands.
5. pca hides slow-mode carriers with the same 2 s grace (pca follow-up).
6. Yes: a message we send to a `joinedVia` contact clears its stranger mark.
