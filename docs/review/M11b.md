# Review M11b — PASS (2026-09-23)

`docs/milestones/M11b.check.sh` → `CHECK PASS: M11b`: meter e2e green without constants (bot-declared hint "with Meter"); coin flip between two identities settled on chain, winner `pcdeceb.89` +0.4981 PAS. Screenshots reviewed: Pocket panel (flat, balances in mono, QR + Copy + Get test funds; footer chip "27.9706 PAS"); flip room (Stake button with wallet icon and amount, stake reference in block, settlement reference).

Answers to docs/questions.md:
- Header order: label first is fine ("your stake: 0 PAS").
- README/kinds for 0008 v2: reviewer updates.
- A stranger's open stake pairs with the next staker: inherent to the contract's single open round; acceptable on devnet; a per-pair room later.
- Two drips in the same second fail: pca faucet should serialize sends (nonce race). Carry to the pca fix list.

Carry: the footer username truncates to "pcdec…" next to the chip — move the chip to the second footer line (fix round or M12); serialize faucet sends in pca.
