# Review M0 — PASS (2026-09-23)

`scripts/check.sh M0` → `CHECK PASS: M0`. Read `src/main/index.ts`, `src/preload/index.ts`, `electron.vite.config.ts`, `docs/decisions.md`.

Decisions accepted:
- `electron-vite@6.0.0-beta.1` stays. Reason: it is the only version that keeps the web client's vite 8 pins. Revisit when 6.0.0 is stable.
- Reviewer fixed the check scripts (`timeout` fallback; any `M<n>:` commit in history). Not the agent's job.

Carry to later milestones (not blocking):
1. Add a Content-Security-Policy meta tag to `src/renderer/index.html` in M3 (packaging).
2. `npm audit` high findings: list them in M3 and decide per package.
