# Review M6 — PASS, engine e2e blocked by this machine's accounts (2026-09-23)

`npm run check` green (38 files, 235 tests, tokens clean). Smoke OK. 14 screenshots reviewed by the reviewer (settings with the Chat section and engine detection, assistant on Claude Code with "tools: off"). The coordinator addition (request draft uses the Composer, Enter sends) verified live by the agent against `pcdpeer.47`.

`docs/milestones/M6.check.sh` fails only at `e2e:engines`: proxy and Claude Code answered; Codex is over its workspace spend cap; OpenCode's `~/.config/opencode/opencode.json` still names `parity-proxy/deepseek-flash`, an alias the proxy retired (current: `auto/deepseek-v4.1-flash`, or `parity/deepseek-v4.1-flash` when the pod is up). Both are outside the repo. Verdict PASS on code; the check re-runs once Shawn fixes either.

Not seen live (specs only): native banner and dock badge, menu on screen, jump-to-bottom, reconnect banner, Not sent · Retry, CLI Stop kill path, OpenCode 1.x success event shape. Carry to the next evaluation session with Shawn.

No fix round. Next: rebuild the dmg from `8e6c306`.
