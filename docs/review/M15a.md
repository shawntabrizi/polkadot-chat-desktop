# Review M15a — PASS on the desktop steps; the bot step waits for pca (2026-09-24)

Commit 9e1b12d. `docs/milestones/M15a.check.sh` → `CHECK PASS: M15a` after the reviewer regenerated the papi descriptors for the new Bulletin chain (`npx papi`; a fresh checkout gets them from `prepare`). 713 tests, tokens clean, smoke; `e2e:attach` on devnet: `//Eve` authorized the sender (100 transactions / 64 MB), a 303 840-byte image stored in one Bulletin transaction (`Stored` in a best block), the message cost one statement, the receiver fetched by bitswap and by the gateway and verified the hash. Bot step `BOT_DESCRIBE_SKIPPED` until the pca half lands. Screenshots reviewed by the agent; the room shows a photo with caption, Open and Save, a download in progress over its blurhash, and an upload in progress.

Accepted: main-process Bulletin I/O (the signer stays in main); progress as a text chip; one image per message (albums in M15b); own BlurHash encoder, no new dependency; the `//Eve` grant also tops up a short balance on devnet.

Answers to docs/questions.md "## M15a":
1. Vectors: pca's codec pins the same A and B bytes; confirmed when its commit lands.
2. Vector A's 12-character blurhash: keep as the malformed-input case (no placeholder painted) and say so in the vectors file (reviewer edits).
3. The C1 store on devnet (block #970869, content hash = chunks[0], fetched by bitswap and gateway) copied into the vectors file by the reviewer.
4. "Open" leaves a copy in the OS temp folder: delete on quit (M15b).
5. Attachment keys in the message row, epoch keys in the groups row: one answer for both in M16b/M15b: a `keys` table next to `secrets`, encrypted at rest with the app's safeStorage key.
6. The stray e2e:attach lines under the M16 acceptance section: the reviewer removes them.
