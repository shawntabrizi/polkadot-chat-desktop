# Review M3 — PASS with one fix (2026-09-23)

`docs/milestones/M3.check.sh` → `CHECK PASS: M3` (check green, `dist/Polkadot Chat-0.1.0-arm64.dmg` 158 MiB, packaged smoke `SMOKE_OK`). Read the report, `docs/decisions.md` M3, `docs/questions.md` M3.

Answers to docs/questions.md:
1. Cache file named by runtime code hash: accepted (a genesis name would serve stale metadata after an upgrade).
2. Profile: the packaged app gets its own profile. Fix item 1 below.

Fix list (commit as `M3: review fixes`, before starting M4):
1. Add `"productName": "Polkadot Chat"` to `package.json` so the packaged app's userData, keychain entry, and window title are separate from `npm run dev`. Confirm `npm run package` and `npm run smoke:packaged` still pass and note the new userData path in README.md ("Polkadot Chat" under Application Support).

Notes for Shawn (not fixes):
- The dmg is unsigned. First open: right-click the app → Open, or `xattr -dr com.apple.quarantine "/Applications/Polkadot Chat.app"`.
- The asar is 100 MB because production node_modules carry the polkadot-api tool chain. Trim in a later milestone (v2 item).
