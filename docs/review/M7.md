# Review M7 — PASS (2026-09-23)

`docs/milestones/M7.check.sh` → `CHECK PASS: M7`: reviewer's own run deleted a message for everyone, the bot on `desktop/rfc-0003` logged `BOT_RECEIVED_DELETED applied:true`, the next reply did not quote the deleted text. Screenshot reviewed: tombstone "Message deleted" (italic, time only) and a dimmed live frame with progress lines.

Facts established:
- `deleted` is kind 21 (20 is `DeviceChatAccepted`). Both implementations pin the same byte vector. RFC-0003 upstream text must change 20 → 21.
- Pre-delivery removal (RFC case 2) is not reachable through `@novasamatech/host-chat` 0.10.2 (`clearOutgoingStatement` drops the whole batch). Cooperative path only; recorded. Worth an SDK request upstream: remove one message from the outgoing batch.

Carry into M7b (small, same area):
1. Treat `pca`'s first placeholder "🤔 One moment — thinking…" as a live frame too (`isLiveFrame`: `⏳ ` or `🤔 ` prefix).
2. Chat-list preview for a live frame shows "Typing…" in `text-fg-tertiary`, never the raw frame text.

Owner items: the Undo grace period loses the deletion if the app quits (accept for now); `pca` could switch its first placeholder to a `⏳` frame on the branch later.
