# Review M11 — PASS (2026-09-23)

`docs/milestones/M11.check.sh` → `CHECK PASS: M11` on the reviewer's run: drip in block 13620420 (9.3 s), dry-run + sign + top-up in block 13620427 (22.7 s), three metered replies 2.2 → 1.9 PAS, references submitted → in block → finalized. Both vectors decode byte for byte. Screenshot reviewed: tx button with wallet icon and amount caption; the signing strip with amount, fee ≈ 0.0009 PAS, signer, dry-run outcome, Cancel / Sign (only primary control while open).

Answers to docs/questions.md:
- Meter address and price as constants in `src/shared/meter.ts`: replace in M11b with the bot-declared balance hint (spec 0008 `balance` field) so the header follows the bot, not a constant.
- Faucet cooldown: turned off by default in pca (`f5a5b42`); e2e reruns no longer block.
- kinds.md / README: updated by the reviewer.

Facts: devnet Asset Hub has `Revive.AutoMap`, so `map_account` was never needed; keep the check for chains without it. Fees ≈ 0.0009–0.0014 PAS. "In block" ≈ 2–3 s, "finalized" ≈ 25–30 s.

Next: M11b — coin flip; balance chip + Pocket panel; bot-declared balance hint; then M12 groups.
