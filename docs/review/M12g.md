# Review M12g — PASS (2026-09-24)

`docs/milestones/M12g.check.sh` → `CHECK PASS: M12g` (566 tests on the combined tree, smoke, typing e2e). Live on devnet between two test identities: a request of 0.2 PAS paid by the peer (dry-run, sign, block #13629688), the requester's bubble turned "Paid" only after the chain showed the transfer, balance delta 0.2 PAS; an over-balance send refused with "Not enough PAS: … available after fees."; a direct send of 0.1 PAS received in block #13629690. Screenshots reviewed: send row, request bubble with Pay and Decline, paid bubble.

History note: M12g's code sits across b9b0c88, 88043de and bdd0e86 because three actors shared one working tree and `git commit` takes the whole index. Rule from now: concurrent agents get their own worktree; the coordinator commits with a pathspec.

Answers to docs/questions.md "## M12g":
1. Spec 0007 `note` conventions: `req:<messageId> <words>` for a request's payment and `Sent <amount> PAS[ · <words>]` for a direct send → added to spec 0007 by the reviewer.
2. The incoming send bubble should also verify on chain before it reads as money received (same check as Paid); until then show "sent you 0.1 PAS · checking…". Next fix round.
3. The existential-deposit rule applies to bot `tx` buttons too: correct.
4. A second device paying the same request twice: acceptable for v1; the requester's bubble stays "Paid" once; note in spec 0007 Unresolved.
5. Keep "Pay 0.5 PAS" as the label and drop the amount from the caption beside it (one number). Next fix round.
6. Worktrees: yes (memory saved).
