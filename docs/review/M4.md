# Review M4 — PASS with one fix (2026-09-23)

`docs/milestones/M4.check.sh` → `CHECK PASS: M4` (check green, 30 files / 188 tests; `e2e:assistant` streamed "proxy ok" through the proxy with `auto/deepseek-v4.1-flash`). The key never appears in Dexie, logs, or repo files; 401 bodies are suppressed because the proxy echoes a masked key.

Answers to docs/questions.md:
1. Markdown in contact rooms: yes. Bots write markdown; the web client's v1 goal was "render bot text as markdown". Fix item 1.
2. Reasoning display: "Thinking…" is enough for v1.

Fix list (commit as `M4: review fixes`):
1. In `Room.tsx`, render incoming text from contacts through the same sanitized markdown renderer used for the Assistant (own messages stay plain). Keep the existing spec pattern; add one spec that a contact message with `**bold**` renders `<strong>` and that raw HTML is stripped.

v1 (M0–M4) is complete after this fix. v2 candidates, in order: markdown `/commands` as buttons (Polkadot Desktop's renderer rules), contract peers (ideas/contracts-as-chat-bots), password-derived backup, bundle size (100 MB asar), Windows/Linux targets.

Fix applied in `abb6b2b`. dmg rebuilt from it by the reviewer. v1 closed.
