# Review M18 — PASS (2026-09-24)

Commit 7851564. `docs/milestones/M18.check.sh` → `CHECK PASS: M18` (832 tests, tokens clean, smoke; `e2e:profiles` on devnet: two profiles in two processes, each its own identity, the same-profile lock refused a second copy, b messaged a over the network, `PROFILES_OK`; packaged smoke migrated the old layout: `PROFILE_MIGRATED`, `MIGRATE_OK`, `AGENT_SELFTEST_OK`). Screenshots: picker and Settings › Profiles, both themes.

Accepted: the user data folder is pointed at `profiles/<name>` before start-up so every module stays unchanged; Electron's own single-instance lock is per profile; "Open" in this window restarts into the profile; the picker is its own process; the badge is per process (no inter-process messaging).

Answers to docs/questions.md "## M18":
1. "Show recovery phrase" before profiles ship: yes, a Settings › Security item that reveals the phrase after a confirm-by-typing step; next round.
2. "Default at launch" = ask, or open one without asking: correct.
3. Invite links land in whichever running process macOS picks: the receiving process asks "Join with which profile?" when more than one profile exists; next round.
4. Dock tiles and badges with two windows: for the owner to check by eye.
