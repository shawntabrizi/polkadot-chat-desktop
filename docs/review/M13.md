# Review M13 — PASS on main; packaged verification pending (2026-09-24)

Commit 332d704. `docs/milestones/M13.check.sh` → `CHECK PASS: M13` on main (606 tests, tokens clean, smoke, `e2e:tools` TOOLS_OK: a tool call became a keyboard with no JSON on screen; `e2e:agent` AGENT_OK: a fresh agent identity published from the app was requested by a test identity, greeted, answered with a keyboard, a press answered, one submission per reply, zero typing). Screenshot reviewed: Settings › Agent with the switch, username and Copy, audience, limits, Stop now, log.

Design accepted: pca bot-core reused, pinned at 675f948 and vendored, run in an Electron utility process in bridge mode with the brain in the main process; a chacha20-poly1305 shim from `@noble/ciphers` because Electron's Node lacks the cipher (bytes proven equal). Tool arguments are shaped before validation (bare strings, flat rows); code is refused.

Not done by the agent, now in a follow-up: the packaged app (bot-core inside app.asar, ESM in a utility process) was never run; fixed debug ports let one agent's e2e drive another agent's app.

Answers to docs/questions.md "## M13":
1. Seed through the child environment and bot-core's 0600 state file: acceptable for devnet v1. Before mainnet: bot-core gets a keyed state store (pca item).
2. With a real engine the seen cost a second submission: bots hold the seen until the turn ends (pca fix in flight); the client's local "working" state covers the wait.
3. `noAllowance` on a fresh account: under investigation (pca).
4. Spec 0006 leniency: yes, name the tool path and bare-string buttons (reviewer adds).
5. "Choose one:" when the model sends buttons without words: keep.
6. `/about` must not reveal the model name to strangers (follow-up).
7. Packaging: follow-up with a packaged e2e.
