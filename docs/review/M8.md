# Review M8 — PASS (2026-09-23)

`docs/milestones/M8.check.sh` → `CHECK PASS: M8`: the guide bot answered "menu" with a kind-242 keyboard (Staking, Governance, Colour of the day, Docs), the desktop pressed the callback, the bot answered "Violet — #EE82EE". Both pca vectors decode byte for byte in the desktop codec. Screenshot reviewed: keyboard inside the bubble, pressed callback with spinner, command, url with host confirm strip, disabled `tx`.

Reviewer ruling applied in `scripts/e2e-buttons.mjs`: the proof is any reply to the press; whether it names the label is reported, not required (the persona decides wording).

Answers to docs/questions.md:
- Over-limit keyboards: desktop truncates, pca rejects. Align on **reject** (spec says limits; a truncated keyboard misrepresents the bot). Next fix round.
- Edit of a buttons message keeps the old rows: accepted for now; spec 0006 already says an `edited` MAY replace rows, which needs an `edited` variant carrying rows. Spec item for 0006 v2.
- Empty request opener → "couldn't reach my agent": pca bug already recorded.

Not seen live: Open on the url strip, the `tx` tooltip, a command press in the UI, Assistant-authored buttons. Owner session.
